DDL
CREATE TABLE 테이블명 ([ 컬럼명 자료형 ])
DROP TABLE 테이블명
ALTER TABLE 테이블명 ADD/MODIFY/DROP

DML
INSERT INTO테이블명(컬럼명)VALUES(값들) 
SELECT 컬럼들 FROM 테이블명 WHERE 조건
UPDATE 테이블명 SEET 변경사항 WHERE 조건
DELETE FROM 테이블명 WHERE 조건
commit / rollback 
TRUNCATE : 데이터만 날림

DISTINCT (중복)

ORDER BY () DESC;
ORDER BY () ASC;

LOWER (str)
알파벳을 소문자로 변환

UPPER (str)
알파벳을 대문자로 변환

INITCAP (str)
첫번째 글자만 대문자로 변환

CONCAT (str1, str2)
두 문자열을 연결

SUBSTR (str, i, n)
문자열 중 i번째에서 n개의 일부 문자를 선택

INSTR (str, ch, n, i )
문자열 중에서 ch 문자열이 n번째부터 i번째있는 위치

LENGTH (str)
문자열의 길이

LPAD / RPAD (str, n, ch)
n 자리수 만큼 확보 후 빈 공백에 특정 문자로 채움

TRIM / LTRIM / RTRIM 
문자를 제거  (*)양쪽 공백 제거에 사용

TRANSLATE ( column, str1, str2 )
문자열에서 str1을 str2로 대체

REPLACE ( column, str1, str2 )
문자열에서 str1을 str2로 대신

SELECT sysdate FROM dual;
오늘

시스템의 날짜를 가져오는 함수 : 
SYSDATE, CURRENT_DATE, SYSTIMESTAMP, 

요일
SELECT to_char(hiredate,'DAY') FROM emp;

SELECT FROM WHERE GROUP BY HAVING(그룹조건) ORDER BY 